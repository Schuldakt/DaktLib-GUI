// =============================================================================
// DaktLib-GUI Version Information
// =============================================================================
// Auto-generated by CMake - DO NOT EDIT
// =============================================================================

#pragma once
#include <cstdint>
#include <string_view>

namespace dakt::gui {

// Version components
inline constexpr int VERSION_MAJOR = @PROJECT_VERSION_MAJOR @;
inline constexpr int VERSION_MINOR = @PROJECT_VERSION_MINOR @;
inline constexpr int VERSION_PATCH = @PROJECT_VERSION_PATCH @;

// Combined version number: MAJOR * 10000 + MINOR * 100 + PATCH
inline constexpr int VERSION_NUMBER = @DAKTLIB_GUI_VERSION_NUMBER @;

// Version string: "MAJOR.MINOR.PATCH"
inline constexpr std::string_view VERSION_STRING = "@PROJECT_VERSION@";

// Build information
inline constexpr std::string_view BUILD_TIMESTAMP = __DATE__ " " __TIME__;

// Platform detection (set by CMake, usable at runtime)
#if defined(DAKTLIB_PLATFORM_WINDOWS)
inline constexpr std::string_view PLATFORM_NAME = "Windows";
#elif defined(DAKTLIB_PLATFORM_MACOS)
inline constexpr std::string_view PLATFORM_NAME = "macOS";
#elif defined(DAKTLIB_PLATFORM_LINUX)
inline constexpr std::string_view PLATFORM_NAME = "Linux";
#else
inline constexpr std::string_view PLATFORM_NAME = "Unknown";
#endif

// Architecture detection
#if defined(DAKTLIB_ARCH_X64)
inline constexpr std::string_view ARCH_NAME = "x64";
#elif defined(DAKTLIB_ARCH_X86)
inline constexpr std::string_view ARCH_NAME = "x86";
#elif defined(DAKTLIB_ARCH_ARM64)
inline constexpr std::string_view ARCH_NAME = "ARM64";
#else
inline constexpr std::string_view ARCH_NAME = "Unknown";
#endif

// Backend availability (compile-time checks)
#if defined(DAKTLIB_ENABLE_VULKAN)
inline constexpr bool HAS_VULKAN = true;
#else
inline constexpr bool HAS_VULKAN = false;
#endif

#if defined(DAKTLIB_ENABLE_DX11)
inline constexpr bool HAS_DX11 = true;
#else
inline constexpr bool HAS_DX11 = false;
#endif

#if defined(DAKTLIB_ENABLE_DX12)
inline constexpr bool HAS_DX12 = true;
#else
inline constexpr bool HAS_DX12 = false;
#endif

#if defined(DAKTLIB_ENABLE_OPENGL)
inline constexpr bool HAS_OPENGL = true;
#else
inline constexpr bool HAS_OPENGL = false;
#endif

#if defined(DAKTLIB_ENABLE_METAL)
inline constexpr bool HAS_METAL = true;
#else
inline constexpr bool HAS_METAL = false;
#endif

// Utility Functions
[[nodiscard]] constexpr auto getVersionString() noexcept -> std::string_view { return VERSION_STRING; }

[[nodiscard]] constexpr auto checkVersion(int major, int minor, int patch = 0) noexcept -> bool {
    const int required = major * 10000 + minor * 100 + patch;
    return VERSION_NUMBER >= required;
}

} // namespace dakt::gui